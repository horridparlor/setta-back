Get started

-In same folder you have this project, "/setta-back" there needs to be "setta-front" where the Reatc UI is and "setta-assets/" that is stored other place than Git.
-Run with "sudo docker-compose up", docker project.
//Todo, run React Frontend also from Docker, so that simplifies everything and the Exporting errors disappear

//Todo, Update Readme
